// total supply equals total balances
unequalBalance(s,n) :- totalBalances(s), totalSupply(n), s!=n.

// no negative balances or allowances
negativeBalance(p,n) :- balanceOf(p,n), n<0, p!=0.
negativeAllowance(s,p,n) :- allowance(s,p,n), n<0, s!=0, p!=0.

// authority rules (retain recv_tx)
pauseNotByPauser() :- recv_pause(), owner(p), msgSender(s), s!=p.
unpauseNotByPauser() :- recv_unpause(), owner(p), msgSender(s), s!=p.
unauthorizedMint() :- recv_mint(_,_), owner(o), msgSender(s), s!=o.
unauthorizedBurn() :- recv_burn(_,_), owner(o), msgSender(s), s!=o.

// (removed: sentinel-based transferWhilePaused)
