// Preserve original property: released amount must not exceed proportional share
overpaid(p,e) :-
    totalReceived(r),
    shares(p,m),
    totalShares(s),
    released(p,e),
    s>0,
    e > r * m / s.

totalReleasedExceedsReceived() :-
    totalReleased(r),
    totalReceived(n),
    r > n.

